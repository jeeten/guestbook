framework:
    test: true
    session:
        storage_id: session.storage.mock_file
        # storage_id: session.storage.php_bridge
        # handler_id: null
        # cookie_secure: auto
        # cookie_samesite: lax
